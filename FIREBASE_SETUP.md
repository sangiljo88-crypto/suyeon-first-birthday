# 🔥 Firebase 방명록 설정 가이드

## ✅ Firebase 설정 완료!

Firebase 설정 코드가 HTML 파일에 적용되었습니다! 
이제 **Firestore 데이터베이스만 활성화**하면 방명록이 작동합니다.

---

## 📋 남은 단계: Firestore 데이터베이스 설정

### 1단계: Firebase Console 접속

1. [Firebase Console](https://console.firebase.google.com/) 접속
2. 프로젝트 선택: **"sooyeon-s-first-birthday"**

### 2단계: Firestore Database 만들기

1. 왼쪽 메뉴에서 **"Firestore Database"** 클릭
2. **"데이터베이스 만들기"** 버튼 클릭
3. 모드 선택: **"테스트 모드에서 시작"** 선택 ⚠️ 중요!
4. 위치 선택: **"asia-northeast3 (Seoul)"** 선택
5. **"사용 설정"** 클릭

> ⏱️ 데이터베이스 생성에 1-2분 정도 소요됩니다.

### 3단계: 보안 규칙 설정

데이터베이스가 생성되면:

1. **"규칙"** 탭 클릭
2. 기존 규칙을 **아래 규칙으로 전체 교체**:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /guestbook/{document} {
      // 모든 사람이 방명록 읽기 가능
      allow read: if true;
      
      // 방명록 작성 규칙
      allow create: if request.resource.data.keys().hasAll(['name', 'message', 'timestamp'])
                    && request.resource.data.name is string
                    && request.resource.data.message is string
                    && request.resource.data.name.size() > 0
                    && request.resource.data.message.size() > 0
                    && request.resource.data.name.size() <= 50
                    && request.resource.data.message.size() <= 500;
    }
  }
}
```

3. **"게시"** 버튼 클릭

### 보안 규칙 설명

위 규칙은 다음을 보장합니다:
- ✅ 누구나 방명록을 읽을 수 있음
- ✅ 이름과 메시지 필수 입력
- ✅ 이름 최대 50자, 메시지 최대 500자
- ✅ 빈 메시지 차단
- ❌ 방명록 수정/삭제 불가 (안전성)

---

## 🧪 테스트 방법

### 로컬에서 테스트

1. `baby_milestone.html` 파일을 브라우저로 엽니다
2. 방명록 섹션으로 스크롤
3. 이름과 메시지를 입력하고 "메시지 남기기" 클릭
4. 메시지가 바로 아래에 표시되면 성공! ✅

### 브라우저 콘솔 확인

문제가 있을 경우:
1. `F12` 또는 `Ctrl+Shift+I` (Mac: `Cmd+Option+I`)
2. Console 탭에서 오류 메시지 확인

---

## 🚨 문제 해결

### "Firebase 초기화 오류" 메시지가 나올 때

**원인**: Firestore 데이터베이스가 아직 활성화되지 않음

**해결**:
1. Firebase Console → Firestore Database
2. "데이터베이스 만들기" 완료했는지 확인
3. 테스트 모드로 시작했는지 확인

### "권한 거부" 오류가 날 때

**원인**: Firestore 보안 규칙이 올바르지 않음

**해결**:
1. Firebase Console → Firestore Database → 규칙 탭
2. 위의 보안 규칙을 정확히 복사해서 붙여넣기
3. "게시" 클릭
4. 페이지 새로고침

### 메시지가 저장되지 않을 때

**확인 사항**:
- [ ] Firestore Database가 활성화되었나?
- [ ] 보안 규칙이 올바르게 설정되었나?
- [ ] 인터넷 연결이 되어 있나?
- [ ] 브라우저 콘솔에 오류가 있나?

---

## 📊 Firebase 사용량 확인

### 무료 플랜 한도

Firebase Spark(무료) 플랜:
- **문서 읽기**: 하루 50,000건
- **문서 쓰기**: 하루 20,000건
- **저장 용량**: 1GB

돌잔치 방명록 정도는 무료 플랜으로 충분합니다! 👍

### 사용량 확인 방법

1. Firebase Console 홈
2. 왼쪽 하단 **"사용량 및 결제"** 클릭
3. Firestore 사용량 확인

---

## 🎉 완료!

모든 설정이 끝났습니다!

### 최종 체크리스트

- [x] Firebase 프로젝트 생성 ✅
- [x] HTML에 Firebase 설정 적용 ✅
- [ ] Firestore Database 활성화 (테스트 모드) ⬅️ 이것만 하면 됩니다!
- [ ] 보안 규칙 설정
- [ ] 로컬 테스트
- [ ] GitHub + Netlify 배포

이제 GitHub에 올리고 Netlify로 배포하면 QR 코드로 공유할 수 있어요! 🚀

---

## 💡 추가 팁

### 배포 후 도메인 사용

Netlify에서 제공하는 URL:
```
https://suyeon-birthday-abc123.netlify.app
```

원하시면 Netlify에서 무료로 커스텀 도메인도 연결할 수 있어요!

### 방명록 관리

Firebase Console → Firestore Database → 데이터 탭에서:
- 작성된 모든 메시지 확인 가능
- 부적절한 메시지 삭제 가능
- 실시간으로 새 메시지 확인 가능

### 보안 강화 (선택사항)

나중에 보안을 더 강화하고 싶다면:
- 작성 속도 제한 (Rate limiting)
- 욕설 필터링
- IP 기반 차단 등 추가 가능

---

궁금한 점 있으시면 언제든 물어보세요! 😊
